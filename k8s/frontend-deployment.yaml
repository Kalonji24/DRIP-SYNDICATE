apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: drip-syndicate
  labels: { app: frontend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: frontend }
  template:
    metadata:
      labels: { app: frontend }
    spec:
      containers:
        - name: frontend
          image: ghcr.io/OWNER/drip-syndicate-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            # Public API URL is baked at build time; this is a runtime fallback only.
            - name: NEXT_PUBLIC_API_URL
              value: "https://api.dripsyndicate.com/api/v1"
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
          readinessProbe:
            httpGet: { path: /, port: 3000 }
            initialDelaySeconds: 10
            periodSeconds: 10
